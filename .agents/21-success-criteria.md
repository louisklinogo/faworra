# Success Criteria

Each PR should:
- Pass `bun run typecheck` and `bun run lint`.
- Follow Server/Client component patterns with initialData.
- Include `team_id` filtering in all queries.
- Use tRPC for API calls; avoid unscoped direct access.
- Update relevant documentation where needed.
- Be tested manually (until automated coverage expands).
